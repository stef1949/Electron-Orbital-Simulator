{
  "name": "electron-orbital-simulator",
  "private": true,
  "type": "module",
  "version": "0.0.0",
  "scripts": {
    "test": "npm run build && node --test",
    "build": "tsc -p tsconfig.json",
    "watch": "tsc -w -p tsconfig.json"
  },
  "devDependencies": {
    "typescript": "^5.9.2"
  }
}
